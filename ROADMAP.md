## Movie Discovery App (Angular) — поэтапный план

### 0. Цели проекта
- **Основная цель (портфолио)**: показать продакшн‑подход к Angular SPA — архитектура по фичам, core/shared, state management (signals), работа с внешним API, тесты, производительность, DX.
- **Дополнительная цель (домашний онлайн‑кинотеатр)**: удобный поиск, красивые карточки фильмов, избранное, бесконечный скролл, тёмная тема.
- **Бонус (монетизация)**: предусмотреть место под блок с рекламой/партнёрскими ссылками/донатами, но внедрять после готового MVP.

Рекомендуемые внешние сервисы:
- **Каталог фильмов**: TMDB API или аналогичный бесплатный API.
- **Хостинг фронта**: Vercel или Netlify (free tier).
- **(Опционально позже)**: Firebase/Auth0 для аутентификации, Stripe/Boosty/Patreon для донатов.

---

### Этап 1. Инициализация проекта
- [x] Создать Angular‑проект через `Angular CLI`.
- [ ] Настроить структуру **feature-based** (папки `core/`, `shared/`, `features/`).
- [ ] Подключить **ESLint + Prettier**.
- [ ] Настроить **alias‑пути** (tsconfig paths).
- [ ] Настроить **environments** (`dev` / `prod`) и хранение API key.
- [ ] Сделать базовый **layout** (`ShellComponent`): шапка, область контента, футер (опционально место под блок монетизации).

---

### Этап 2. Архитектура ядра и shared‑слоя

#### 2.1 Core layer
- [ ] Создать папку `core/`.
- [ ] Реализовать `ApiInterceptor` (подстановка base URL и API key).
- [ ] Реализовать `ErrorInterceptor` (единая обработка HTTP‑ошибок).
- [ ] Добавить глобальные сервисы: `LoggerService`, `ConfigService` и т.п.

#### 2.2 Shared layer
- [ ] Создать папку `shared/`.
- [ ] Реализовать UI‑компоненты:
  - [ ] `Button`
  - [ ] `Loader` (spinner + skeleton)
  - [ ] `EmptyState`
- [ ] Реализовать pipes:
  - [ ] `DateFormatPipe`
  - [ ] `ImageFallbackPipe`

---

### Этап 3. Data Access Layer

#### 3.1 MovieService
- [ ] Создать сервис `MovieService`:
  - [ ] `searchMovies(query, page)` — поиск с пагинацией.
  - [ ] `getMovie(id)` — детали фильма.

#### 3.2 Кеширование и хранилище
- [ ] Реализовать **кеш‑слой**:
  - [ ] Map‑кеш по ключу (query + page / id).
  - [ ] TTL (опционально).
  - [ ] Использование `shareReplay`.
- [ ] Реализовать `StorageService` (обёртка над `localStorage`/`sessionStorage`):
  - [ ] `get()`
  - [ ] `set()`
  - [ ] `remove()`

---

### Этап 4. State Management (Signals + Facade)
- [ ] Создать `MovieStore` на сигналах:
  - [ ] `movies` signal
  - [ ] `loading` signal
  - [ ] `error` signal
  - [ ] `selectedMovie` signal
- [ ] Реализовать методы стора:
  - [ ] `search(query)`
  - [ ] `loadMovie(id)`
  - [ ] `loadNextPage()`
- [ ] Создать `MovieFacade`:
  - [ ] Публичные сигналы/observable: `movies$`, `loading$`, `error$`, `selectedMovie$`
  - [ ] Методы прокидывают вызовы к стору и сервису.

---

### Этап 5. Feature: Movies (базовый модуль фильмов)
- [ ] Создать `features/movies/`:
  - [ ] `data-access/` — использование `MovieService`, маппинг DTO → view‑model.
  - [ ] `ui/` — карточки фильмов.
  - [ ] `feature-search/` — контейнер поиска.
  - [ ] `feature-details/` — контейнер деталей.
  - [ ] `feature-favorites/` — контейнер избранного.

---

### Этап 6. Feature: Search

UI:
- [ ] Страница `/search`.
- [ ] `Search input` (Reactive Forms).
- [ ] Список фильмов (использует shared‑компоненты).
- [ ] Loader и Empty state.

Логика:
- [ ] RxJS‑цепочка с:
  - [ ] `debounceTime`.
  - [ ] `distinctUntilChanged`.
  - [ ] `filter` (минимум 2 символа).
  - [ ] `switchMap` (отмена прошлых запросов).

UX:
- [ ] Skeleton loading.
- [ ] Обработка ошибок (видимый, но аккуратный error‑state).
- [ ] Нет «пустых» переходов без результата.

---

### Этап 7. Feature: Movie Details
- [ ] Страница `/movie/:id`.
- [ ] Resolver для предварительной загрузки данных фильма.
- [ ] UI:
  - [ ] Постер.
  - [ ] Название.
  - [ ] Жанры.
  - [ ] Описание.
  - [ ] Рейтинг.
- [ ] Кнопка «добавить в избранное».
- [ ] Обработка ошибок/нет данных.

---

### Этап 8. Feature: Favorites
- [ ] Хранить избранное через `StorageService`.
- [ ] Кнопка‑переключатель избранного (иконка ♥) на карточке и в деталях.
- [ ] Страница `/favorites`:
  - [ ] Список сохранённых фильмов.
  - [ ] Возможность удалить из избранного.
  - [ ] Empty state, если список пуст.

---

### Этап 9. Routing
- [ ] Настроить lazy‑loading модулей:
  - [ ] `/search`
  - [ ] `/movie/:id`
  - [ ] `/favorites`
- [ ] Настроить preloading strategy (например, preloading details/favorites после первого поиска).
- [ ] Подключить resolver для деталей.

---

### Этап 10. Infinite Scroll
- [ ] Реализовать `IntersectionObserver`‑директиву/сервис.
- [ ] Добавить подгрузку следующих страниц при достижении низа списка.
- [ ] Показать loader при догрузке.
- [ ] Позаботиться о производительности (trackBy, OnPush).

---

### Этап 11. Performance
- [ ] Включить `ChangeDetectionStrategy.OnPush` для контейнеров и list‑компонентов.
- [ ] Реализовать `trackBy`‑функции для `*ngFor`.
- [ ] Проверить lazy‑loading всех тяжёлых модулей и изображений.
- [ ] Оптимизировать размер изображений (quality/size, `srcset`/`sizes` при необходимости).

---

### Этап 12. UI / UX полировка
- [ ] Добавить адаптивную сетку (мобильный/планшет/desktop).
- [ ] Hover‑эффекты и небольшие анимации.
- [ ] Skeleton loaders + shimmer‑эффект.
- [ ] Тёмная тема и переключатель темы.
- [ ] Красивые error и empty states.
- [ ] Место под небольшой промо‑блок (для будущей монетизации).

---

### Этап 13. Error Handling
- [ ] Глобальный HTTP‑interceptor для ошибок.
- [ ] Единый компонент/сервис для показа ошибок (toast/баннер).
- [ ] Возможность «повторить попытку» там, где это оправдано.

---

### Этап 14. Тестирование

Unit tests:
- [ ] `MovieService`.
- [ ] `MovieStore`.
- [ ] `StorageService`.

Component tests:
- [ ] Search feature.
- [ ] Movie details.
- [ ] Favorites.

---

### Этап 15. Dev Experience
- [ ] Husky (pre‑commit hook).
- [ ] `lint-staged` для форматирования/линта только изменённых файлов.
- [ ] Стиль коммитов (conventional commits).

---

### Этап 16. Деплой
- [ ] Настроить production build.
- [ ] Деплой на Vercel или Netlify.
- [ ] Проверить/настроить environment variables (API key и т.п.).
- [ ] Протестировать работу приложения в продакшн‑окружении.

---

### Этап 17. README и портфолио
- [ ] Написать подробный `README`:
  - [ ] Описание проекта и целей.
  - [ ] Используемые технологии и подходы (Angular, signals, interceptors, feature‑архитектура, RxJS, тесты и т.д.).
  - [ ] Инструкция по запуску/сборке/деплою.
  - [ ] Скриншоты/гифки.
- [ ] Кратко описать, как проект демонстрирует твои Angular‑скиллы (для HR/тимлидов).

---

### Идеи для следующих итераций (не обязательно для первого релиза)
- [ ] **Аутентификация и профили пользователей** (Firebase/Auth0).
- [ ] **Списки «хочу посмотреть» / истории просмотра**.
- [ ] **Реальный плеер** с интеграцией с легальными источниками/твоим NAS.
- [ ] **Монетизация**:
  - [ ] Нативный промо‑блок с рекомендацией контента/подборок.
  - [ ] Партнёрские ссылки (например, на покупку подписки в другом сервисе).
  - [ ] Блок донатов (Stripe/Boosty/Patreon).

